<%= form_for([@article, @article.comments.build], remote: true) do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <% if user_signed_in? %>
    
  <% else %>
    <div class="field">
      <%= f.label :email %><br>
      <%= f.email_field :email, class: 'form-control', id: 'comment_email' %>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :leave_a_comment %><br>
    <%= f.text_area :body, class: 'form-control', id: 'comment_body' %>
  </div>
  <div class="actions">
    <%= f.submit "Submit" , class:'btn btn-primary' %>
  </div>
<% end %>
